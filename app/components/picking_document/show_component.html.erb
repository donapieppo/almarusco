<% if @picking_document %>  
  (da formulario <%= @picking_document.serial_number %>):
  <%= @picking_document.kgs %> Kgs.
  <%= @picking_document.volume %> L.
  <%= link_to 'modifica', [:edit, @picking, @picking_document], class: 'btn btn-sm btn-primary' %> 
<% else %>
  <center>
    <%= link_to 'Aggiungi documento (formulario)', new_picking_picking_document_path(@picking, disposal_type_id: @disposal_type.id), class: 'btn btn-primary btn-sm' %>  
  </center>
<% end %>

