<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Gestione Rifiuti Speciali. Università di Bologna.">
    <meta name="author" content="Dipartimento di Matematica - Unibo - Pietro Donatini">
    <title>AlmaRusco - Gestione Rifiuti Speciali - Università di Bologna</title>

    <%= dm_unibo_common_headers %>  
  </head>
  <body>
    <%= render partial: 'layouts/menu' %>

    <main role="main" class="container">
      <%= turbo_frame_tag "modal" %>
      <%= bootstrap_flash %>

      <% if current_organization %>  
        <h2 class="centered">
          <%= current_organization.code %> - <%= current_organization.name %>
        </h2>
      <% end %>

      <%= yield %>
    </main><!-- container -->

    <%= render DmUniboCommon::FooterComponent.new(current_user, contacts_path: contacts_path, documentation_path: "https://www.dm.unibo.it/almarusco/help/docs/intro/") %>  
  </body>
</html>
