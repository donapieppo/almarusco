<h1>Codici UN</h1>

<div class="alert alert-warning" role="alert">
  I codici UN sono <b>comuni a tutte le strutture</b>.<br/>
  Vi preghiamo di inserire/aggiornare questi dati con cura.
</div>

<% if policy(:un_code).new? %>  
  <div class="centered my-4">
    <%= link_to 'Aggiungi nuovo codice UN', new_un_code_path, class: 'btn btn-primary' %>  
  </div>
<% end %>

<div class="list-group">
  <% @un_codes.each do |un| %>  
    <% url = policy(un).edit? ? edit_un_code_path(un) : '#' %>  
    <%= link_to url, class: "list-group-item list-group-item-action" do %>
      <strong><%= un.id %></strong> 
      <span class="ps-2"><%= un.name %></span>
    <% end %>
  <% end %>
</div>
