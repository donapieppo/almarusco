const disposalTypeSelectorForm = document.querySelector("form.status_selector");

disposalTypeSelectorForm.addEventListener('change', (e) => {
  let dispoalTypeStatus = e.target.id

  document.querySelectorAll(".choosable-disposal-type").forEach((x) => {
    if (x.classList.contains(dispoalTypeStatus)) {
      x.style.display = 'block';
    } else {
      x.style.display = 'none';
    }
  });
  document.querySelectorAll(".disposal-card").forEach(d => {
    d.style.display = (d.dataset.disposaltypeid === selectedId) ? 'block' : 'none';
  })
});

